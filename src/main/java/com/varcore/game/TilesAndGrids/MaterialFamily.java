package com.varcore.game.TilesAndGrids;

/**
 * Optional label for organizing materials in the registry.
 * Expansion / dominance is decided only by layer + expand flags, not by family.
 */
public enum MaterialFamily
{
    EMPTY,
    DIRT,
    GRASS,
    STONE
}
